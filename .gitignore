# Squad: keep entire team state local — do not publish to remote
.squad/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# Agent cache — binaries and models are never committed
cache/
cache/*
